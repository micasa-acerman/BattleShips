export const GRID_SIZE = 24;
export const GRID_SEPARATOR_WIDTH = 1;
export const OPTIONS_SAMPLES_OF_SHIPS: number[] = [
  ...new Array(1).fill(4),
  ...new Array(2).fill(3),
  ...new Array(2).fill(2),
  ...new Array(4).fill(1),
];
